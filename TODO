replace menu by message box
if click on select, a tools box open and ask to choose the mode
same for color and other

add sound recorder
can be use undrowish if have not lib
undrowish is apte via the console
snack demo:
  package require sound

  set ::left  90
  set ::right 90
   
  snack::mixer volume IGain left right

  snack::sound ss -channel stdout -rate 16000 -channels Mono
  ss record -input Line1 -fileformat WAV

  after [expr [lindex $argv 0] * 1000] exit

  vwait forever

find lib for windows and linux to resize img

add activity recoder